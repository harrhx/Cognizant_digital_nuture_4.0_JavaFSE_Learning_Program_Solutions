public class StudentView {

    public void studentdisplay(String name,String id,String grade){
        System.out.println("The name of the studnet is " + name + " Id id " + id + " grade is " + grade);
    }
}